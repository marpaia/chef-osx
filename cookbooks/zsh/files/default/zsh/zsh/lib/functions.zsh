cdd(){ cd ~/Desktop/; }

cdg(){ cd ~/github/; }

cdgo(){ cd $GOPATH; }

gopath(){ cd $GOPATH; }

gop(){ cd $GOPATH; }

dope(){ echo "no doubt"; }

t(){ tail -f $1 | lolcat; }

ff(){ find ./ -name *$1* 2>/dev/null; }
